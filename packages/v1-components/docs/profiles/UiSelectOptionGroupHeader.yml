component: UiSelectOptionGroupHeader
summary: >
  UiSelectOptionGroupHeader is the header element used for labeled grouped sections
  inside select dropdown content.

public_import:
  from: '@retailcrm/embed-ui-v1-components/remote'
  named:
    - UiSelectOptionGroupHeader

related_components:
  - UiSelectOptionGroup

use_when:
  - You need a visible group heading inside select option content.

avoid_when:
  - You do not need separate group labeling.

provenance:
  implementation:
    - src/remote/components/select/UiSelectOptionGroupHeader.vue
