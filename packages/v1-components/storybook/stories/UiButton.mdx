import ToReact from '../ToReact.ts'
import UiButton from './UiButton.example.vue'

# UiButton

Стандартная кнопка в системе

<p style={{ display: 'flex', gap: '16px' }}>
  <ToReact tag={'span'} is={UiButton} text={'Create'} appearance={'primary'} />
  <ToReact tag={'span'} is={UiButton} text={'Create'} appearance={'secondary'} />
  <ToReact tag={'span'} is={UiButton} text={'Create'} appearance={'tertiary'} />
  <ToReact tag={'span'} is={UiButton} text={'Create'} appearance={'outlined'} />
</p>
